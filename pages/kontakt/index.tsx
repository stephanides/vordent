import { Contact } from '../../app-data/components/web/Contact';
import { SiteLayout } from '../../app-data/shared/components/SiteLayout';

const KontaktPage: () => JSX.Element = () => (
  <>
    <SiteLayout>
      <Contact />
    </SiteLayout>
  </>
);

export default KontaktPage;
